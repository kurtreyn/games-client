export enum EventEnum {
    INIT = 'init',
    JOIN = 'join',
    MOVE = 'move',
    WIN = 'win',
    ERROR = 'error',
    RESET = 'reset'
}


export enum ConnectFourCellState {
    EMPTY = 'empty',
    RED = 'red',
    YELLOW = 'yellow'
}